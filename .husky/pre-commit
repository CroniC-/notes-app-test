#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint and format checks before commit
npm run lint
npm run format:check

# If you want to auto-fix, uncomment these lines:
# npm run lint:fix
# npm run format
